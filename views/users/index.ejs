<%- include('../partials/header') %>
<p><em>This users web page showcases the creative work of Priscila Garcia-Mendoza. It serves as a demonstration, developed using Node.js and Express.js, along with EJS templates. The project exemplifies a list/view design approach, drawing data from a JSON file. The visual elements and content are generated using third-party APIs, ensuring that they do not involve real identities.</em></p>
<h1 style="margin-top:10%">Users</h1>

 <table class="table table-hover table-striped">
	<thead>
   	<tr>
      	<th scope="col">First Name</th>
      	<th scope="col">Last Name</th>
      	<th scope="col">Email</th>
   	</tr>
	</thead>
 <tbody>

<% users.forEach(function(user){ %>
  <tr>
   	<td><a href="users/view/<%= user.id %>"><%= user.first_name %></a></td>
   	<td><%= user.last_name %></td>
   	<td><%= user.email %></td>
  </tr>
<% }); %>

</tbody>
</table>

<%- include('../partials/footer') %>
